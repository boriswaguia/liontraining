# ============================================================
# LionLearn — Environment Variables
# Copy this file to .env and fill in the required values.
# Never commit .env to git.
# ============================================================

# ── Database ─────────────────────────────────────────────────
# PostgreSQL credentials used by both the db and app containers.
POSTGRES_USER=lionlearn
POSTGRES_PASSWORD=change_me_strong_password   # REQUIRED
POSTGRES_DB=lionlearn

# ── Application ──────────────────────────────────────────────
# Port exposed by docker compose (maps to container port 3000).
APP_PORT=80

# Public URL of the app — used for NextAuth callbacks and email links.
AUTH_URL=https://lionlearning.briskprototyping.com

# NextAuth secret — generate with: openssl rand -base64 32
AUTH_SECRET=change_me_random_secret           # REQUIRED

# ── AI ───────────────────────────────────────────────────────
# Google Gemini API key — https://aistudio.google.com/app/apikey
GEMINI_API_KEY=AIzaSy...                       # REQUIRED

# ── Email (Resend) ────────────────────────────────────────────
# Daily recommendation emails. Leave blank to disable email.
# Get an API key at https://resend.com
RESEND_API_KEY=re_...
# Must match a verified domain in your Resend dashboard.
RESEND_FROM=LionLearn <noreply@yourdomain.com>

# ── Cron ─────────────────────────────────────────────────────
# Secret used to authenticate internal cron requests.
# Leave blank — the app auto-generates one on startup.
CRON_SECRET=
